# 计算机毕业设计LangChain智能体工作流编排-图检索增强(GraphRAG)-桥梁规范与巡检报告智能问答系统-知识图谱(Neo4j)和大模型(LLM)
# LangChain智能体工作流编排-图检索增强(GraphRAG)-桥梁规范与巡检报告智能问答系统-知识图谱(Neo4j)和大模型(LLM)
# 基于知识图谱（Neo4j）和大语言模型（LLM）的图检索增强（GraphRAG）桥梁规范与检测报告系统

## 项目概述

本项目面向桥梁养护、检测与技术管理场景，是一个融合桥梁档案、检测报告、规范文档、知识图谱和大语言模型的智能业务系统。系统采用前后端分离架构，以 SQLite 保存结构化业务数据，以 Neo4j 保存桥梁领域实体关系，以 LangChain 统一接入 OpenAI 兼容的大语言模型端点，并通过 GraphRAG 将知识图谱、桥梁档案、检测报告和来源文档联合用于问答生成。

系统的目标不是替代专业检测结论，而是帮助检测人员、养护人员和管理人员快速定位规范依据、汇总历史检测信息、分析桥梁构件与病害之间的关系，并形成可追溯的辅助回答和巡检计划。涉及结构安全、技术状况等级和处治方案的结果，仍须由具备相应资质的专业人员复核。

当前版本已完成桥梁领域数据模型和界面的整体改造，具备可运行的前端页面、REST API、SQLite 初始化数据、Neo4j 图谱服务、LangChain 模型路由、真实短信服务接入及自动化测试。首次启动会初始化 13 张业务表，并写入 2 个演示用户、5 座桥梁、6 份检测报告、9 份测试文档和演示问答数据。

### 核心创新点

- **桥梁领域知识建模**：使用 18 类节点和 20 类关系描述桥梁、桥型、构件、材料、规范、检测步骤、设备、指标、方法、病害风险等知识。
- **GraphRAG 联合检索**：根据问题抽取关键词和实体，同时检索图谱路径、桥梁档案、检测报告和来源文档，将结果组织为带来源标识的结构化上下文。
- **可追溯回答**：问答消息保存关键词、实际使用的模型、桥梁/报告/文档检索结果和图谱快照；来源文档仅展示相关摘要或片段，可进一步打开详情核验。
- **LangChain 模型路由**：文本模型和视觉模型分开管理，按启用状态及调用顺序依次尝试；支持 GLM、Qwen、魔搭社区等 OpenAI 兼容端点。
- **模型安全管理**：API Key 加密保存且接口不回显；管理员可新增、编辑、启停、测试端点，并通过上移/下移按钮调整持久化调用顺序。
- **语义化三元组抽取**：使用 LLM 和 Pydantic 结构化输出提取三元组，严格限制节点与关系类型；失败时采用桥梁领域规则兜底，不再按照实体出现顺序构造链式关系。
- **图谱构建过程可视化**：全量、增量和单文档建图均采用进程内异步任务，页面实时展示解析、LangChain 路由、LLM 抽取、结构校验、规则兜底和 Neo4j 写入进度。
- **图数据库降级能力**：Neo4j 不可用时明确返回降级状态，并使用 SQLite 中的三元组提供基础图谱查询和可视化，避免页面无反馈。
- **多模态辅助分析**：桥梁现场图片通过 LangChain 视觉模型辅助识别裂缝、支座、桥面、墩台和缆索等可见问题；图片地址随消息持久化，切换会话后仍可恢复。
- **业务闭环**：桥梁档案、检测报告、规范文档、图谱构建、智能问答、巡检计划、问答导出、统计大屏和数据库后台形成完整的数据使用链路。


## 要求
### 源码有偿！一套(论文 PPT 源码+sql脚本+教程)

### 
### 加好友前帮忙start一下，并备注github有偿桥梁检测问答27
### 我的QQ号是2827724252或者微信:code520888 或者 bysj2023nb

# 

### 加qq好友说明（被部分 网友整得心力交瘁）：
    1.加好友务必按照格式备注
    2.避免浪费各自的时间！
    3.当“客服”不容易，repo 主是体面人，不爆粗，性格好，文明人。

## 运行演示视频

https://www.bilibili.com/video/BV1tzg36rEAQ

## 运行截图

![](00.jpeg)
![](0.jpeg)
![](1.png)
![](2.png)
![](3.png)
![](4.png)
![](5.png)
![](6.png)
![](7.png)
![](8.png)
![](9.png)
![](10.png)
![](11.png)
![](12.png)
![](13.png)
![](14.png)
![](15.png)
![](16.png)
![](17.png)
![](18.png)
![](19.png)
![](20.png)
![](21.png)
![](22.png)
![](23.png)
![](24.png)
![](25.png)
![](26.png)
![](27.png)


### 应用场景

#### 用户端功能

- **账号认证**：支持账号密码登录、注册、手机短信验证码登录和短信验证后重置密码。
- **个人中心**：支持查看和修改个人资料、修改密码及上传头像；头像上传成功后同步更新个人中心、导航栏和本地登录状态。
- **智能问答**：支持桥梁规范、检测方法、指标、设备、病害和历史报告等自然语言查询，以及连续多轮对话。
- **图片问答**：支持上传桥梁或检测现场图片进行辅助识别，聊天区展示原始所选图片并持久化保存。
- **会话管理**：支持创建、切换、重命名、清空和删除会话；可调用 LLM 生成标题，在弹窗中查看生成进度并编辑确认后保存。
- **检索依据查看**：右侧检索结果按桥梁档案、检测报告、来源文档、图谱和关键词分组展示；条目可点击弹窗查看详情。
- **问答历史**：支持回看问题、回答、关键词、检索证据、知识图谱快照和模型信息。
- **导出功能**：支持将完整会话导出为 DOCX 文档。

#### 管理端功能

- **控制台与数据大屏**：展示用户、文档、桥梁、报告、会话、消息、节点、关系和建图任务等指标，以及桥型、报告等级、检测类型、文档类型、节点类型、关系类型和系统活跃趋势图表。
- **用户管理**：支持用户查询、新增、编辑、启用/禁用、角色调整和删除；普通用户无法访问管理员接口。
- **模型管理**：分别维护文本与视觉端点，支持密钥只写、连接测试、启停、编辑、删除和调用顺序调整；连接失败时给出针对 Base URL、认证、模型标识、频率限制等问题的可读提示。
- **文档管理**：支持目录树、文件上传、MD5 去重、关键词筛选、内容查看、下载、编辑、删除和 LLM 摘要；左侧目录和右侧内容区可独立滚动。
- **知识图谱管理**：支持单文档、增量和全量构建，查询构建任务进度、成功/失败数量和错误明细，并查看三元组的来源片段及置信度。
- **知识图谱可视化**：支持节点/关系类型过滤、关键词搜索、1—3 跳邻居、力导向/环形布局、连接度统计和节点详情；不同节点类型采用稳定的独立颜色。
- **桥梁档案管理**：维护桥梁编号、名称、桥型、材料、结构体系、路线路段、跨径参数、位置、建成日期、相关规范、说明和图片。
- **检测报告管理**：维护报告编号、关联桥梁、检测日期与类型、路段、点位、人员、设备、方法、指标、费用、等级、结论、建议和多张现场图片。
- **巡检计划**：从桥梁详情汇总历史报告，由 LangChain 生成检查范围、频次、重点构件、方法设备、人员安排、风险控制和规范依据；可保存为知识文档并导出 DOCX。
- **SQLite 管理后台**：通过导航栏头像左侧的“管理后台”按钮进入，仅开放 13 张业务表的白名单查询和受控删除；密码、模型密钥等敏感列不在通用接口中返回。

#### 核心技术功能

- **实体与关键词提取**：从问题中识别桥梁、桥型、规范、桥位部位、指标、设备、检测方法和病害风险等实体。
- **图谱路径检索**：以实体或关键词作为种子检索 1—3 跳邻居，返回节点、关系、类型、连接度和来源信息。
- **多源证据检索**：联合查询 Neo4j/SQLite 图谱、桥梁档案、检测报告和知识文档，并对传入 LLM 的文本长度进行控制。
- **受约束回答生成**：提示词要求模型仅依据检索证据回答，不得虚构规范编号、条款或现场测量结果；证据不足时必须明确说明。
- **流式响应**：`/api/chat/stream` 使用 Server-Sent Events 依次返回检索结果、生成文本片段和完成事件。
- **结构化三元组提取**：通过 LangChain `ChatOpenAI` 与 Pydantic 模型约束 `<头实体, 关系, 尾实体>`，并保留源文档、原文片段和置信度。
- **语义中心构图**：围绕“桥梁/桥型—构件/材料/规范”“检测步骤/方法—设备/指标”“病害—部位/检测方法”等中心建立辐射关系，避免所有节点首尾相连成单链。
- **端点故障切换**：按照模型类型和管理员配置的优先顺序调用端点；端点异常时记录状态和错误并尝试下一个可用端点。
- **安全与权限**：使用 JWT 鉴权、管理员装饰器、密码哈希、模型密钥加密、上传类型与大小校验以及 SQLite 表白名单控制。
- **短信安全控制**：验证码有效期 5 分钟、同号码 60 秒发送冷却、每小时最多 10 次且使用后立即失效；重置密码使用短时一次性重置票据。

## 技术架构

### 整体架构设计

```text
┌──────────────────────────────┐
│ 浏览器 / 响应式管理工作台     │
│ Vue 3 + TypeScript + Element │
│ http://127.0.0.1:3000        │
└──────────────┬───────────────┘
               │ REST / JWT / SSE / 文件上传下载
┌──────────────▼───────────────┐
│ Flask API（Python 3.11+）     │
│ http://127.0.0.1:5010        │
│ 认证、业务服务、GraphRAG、任务 │
└───────┬────────┬────────┬────┘
        │        │        │
┌───────▼───┐ ┌──▼──────┐ ┌▼────────────────────┐
│ SQLite    │ │ Neo4j   │ │ LangChain 模型路由   │
│ 13张业务表 │ │ 18类节点 │ │ GLM / Qwen / 魔搭等  │
│ 三元组降级 │ │ 20类关系 │ │ OpenAI 兼容端点      │
└───────┬───┘ └─────────┘ └──────────┬──────────┘
        │                             │
┌───────▼────────────────┐  ┌────────▼────────────┐
│ 本地文件存储            │  │ 阿里云市场短信接口   │
│ 文档/头像/桥梁/报告/聊天 │  │ 登录与密码重置验证码 │
└────────────────────────┘  └─────────────────────┘
```

主要数据流如下：

1. 管理员上传 TXT、DOCX、PDF 或 XLSX 文档，后端提取正文、计算 MD5 并保存文档元数据。
2. 建图任务对正文分片，优先通过 LangChain 调用文本 LLM 提取结构化三元组；模型不可用或结构校验失败时使用桥梁领域规则兜底。
3. 三元组首先写入 SQLite，并在 Neo4j 可用时同步写入图数据库；每条关系保存来源文档 ID、原文片段和置信度。
4. 用户提问后，系统提取关键词和实体，检索图谱、桥梁档案、检测报告和来源文档，再将受控上下文交给 LangChain 生成回答。
5. 问题、图片、回答、关键词、检索结果、图谱快照和模型标识写入 SQLite，用于会话恢复、详情展示与 DOCX 导出。

### 技术栈详解

#### 前端技术栈

- **Vue 3.5 + Composition API**：实现单页管理工作台和响应式交互。
- **TypeScript**：定义用户、文档、桥梁、报告、模型、图谱及问答数据类型。
- **Element Plus**：提供表单、表格、弹窗、上传、进度条、消息和分页等组件。
- **Pinia**：维护认证和会话状态，并与 `localStorage` 同步登录用户信息。
- **Vue Router**：承载单页应用入口；各业务模块由工作台统一编排。
- **Axios**：封装统一 API 请求、JWT 请求头和错误处理。
- **ECharts 5.6**：实现知识图谱、统计卡片及数据大屏图表。
- **Marked**：将模型返回的 Markdown 安全渲染为问答内容。
- **Vite 6**：提供开发服务器与生产构建；图谱和大屏组件采用异步加载。
- **视觉规范**：依据 `prototype` 原型采用深海蓝、监测青和安全橙配色，并适配 1440、1024、768 和 375 像素宽度。

#### 后端技术栈

- **Flask 2.2**：提供 REST API、文件服务、SSE 流式响应及统一错误处理。
- **SQLite**：保存用户、目录、文档、会话、消息、桥梁、报告、三元组、任务、模型、元数据、反馈和短信验证码。
- **PyJWT + Werkzeug**：实现 JWT 登录态与密码哈希校验。
- **cryptography**：加密保存模型 API Key，读取接口仅返回是否已配置密钥。
- **Flask-CORS**：按环境变量配置允许访问的前端来源。
- **python-docx / pdfplumber / openpyxl**：解析 DOCX、PDF、XLSX，并生成问答记录和巡检计划 DOCX。
- **Requests**：调用阿里云市场短信服务；请求采用 `Authorization: APPCODE ...` 和表单参数协议。
- **进程内线程池**：异步执行建图任务并将进度持久化到 SQLite；当前版本未依赖外部消息队列。
- **本地文件存储**：按 `avatars`、`documents`、`bridge_images`、`inspection_images` 和 `chat_images` 分类保存上传内容。

#### 算法服务技术栈

- **Neo4j 5.x Driver**：建立节点唯一性约束，执行节点/关系合并、全文子图查询、1—3 跳邻居查询、统计和文档图谱查询。
- **LangChain 1.x + langchain-openai**：使用 `ChatOpenAI(model=..., base_url=...)` 统一接入 OpenAI 兼容端点。
- **Pydantic 2.x**：校验实体和三元组结构，限制节点类型、关系类型、置信度和来源片段字段。
- **文本模型路由**：默认模型为 `glm-4.7-flash`，可增加 Qwen、魔搭社区或其他兼容端点作为主模型或备援。
- **视觉模型路由**：默认模型为 `glm-4.6v-flash`，通过 LangChain 多模态消息传递 Base64 图片内容。
- **规则抽取兜底**：内置桥梁、桥型、材料、构件、病害、指标、设备、方法、人员和环境词典，并按实体类型匹配合理关系。
- **SQLite 图谱降级**：Neo4j 连接失败时从 `graph_triples` 重建节点、边、分类和连接度，并在接口中返回 `sqlite_fallback` 与降级标志。

运行时关键配置位于 `backend/.env`，可从 `backend/.env.example` 复制。至少应根据部署环境修改 `SECRET_KEY`、`JWT_SECRET_KEY`、`MODEL_KEY_SECRET`、Neo4j 连接信息、模型 API Key、短信 APPCODE、短信模板 ID 和 CORS 来源。真实密钥不得提交到源码。

## 核心功能模块

1. **认证与用户管理**
   - 密码登录、注册、退出和当前用户查询。
   - 手机验证码发送与短信登录；开发模式可安全返回调试验证码，生产模式禁止回显。
   - 重置密码采用“短信验证码校验 → 获取一次性重置票据 → 提交新密码”两阶段流程。
   - 个人资料、密码和头像维护；头像返回稳定的 `/uploads/avatars/...` 地址。
   - 管理员可维护用户角色和状态，后端对管理员接口统一执行 403 权限校验。

2. **文档与目录管理**
   - 支持 TXT、DOCX、PDF、XLSX 上传解析，单文件最大值默认 50 MB。
   - 通过 MD5 哈希阻止重复文档，保存文件名、格式、大小、来源、正文、摘要和建图状态。
   - 支持目录树新增、编辑、删除、排序和文档归档。
   - 支持文档详情、部分内容预览、原文查看、下载、编辑、删除和 LLM 摘要。
   - 文档页左侧目录和右侧内容区独立滚动，不因长目录或长列表相互影响。

3. **智能问答与会话**
   - 问答流程：实体抽取 → 多源检索 → 结构化上下文 → LangChain 生成 → 证据快照持久化。
   - 最近会话消息随问题传入提示上下文，支持连续追问和上下文理解。
   - 支持普通 JSON 响应和 SSE 流式响应。
   - 会话标题由 LLM 生成建议，生成和保存相互分离，用户可编辑后确认。
   - 上传图片以服务器保存后的真实地址展示，消息重新加载时根据持久化地址恢复。
   - 右侧检索结果中的桥梁、报告和文档均可点击查看详情，文档卡片只显示必要片段。
   - 问答记录支持 1—5 分评价、文字反馈和 DOCX 导出。

4. **GraphRAG 检索与回答**
   - 使用 LangChain 提取问题关键词和实体，失败时使用领域词典兜底。
   - 图谱检索默认扩展 2 跳，接口允许 1—3 跳；桥梁、报告和文档通过结构化字段与正文关键词检索。
   - 提示上下文包含桥梁编号、桥型、路段、跨径、适用规范、报告等级、检测指标、结论、文档摘要和图谱路径。
   - 模型回答必须给出检索结论、证据与分析、检测或处治建议、风险边界和来源编号。
   - 非桥梁领域闲聊或简单上下文查询可跳过检索，减少不必要的模型与数据库调用。

5. **知识图谱构建与管理**
   - 支持单文档同步、增量构建和全量重建。
   - 文档按片段处理，并以“文档解析、LLM 抽取、Pydantic 校验、三元组清洗、SQLite 保存、Neo4j 同步”为进度单位。
   - 建图任务保存状态、总量、进度、成功数、失败数、提示文字、错误明细及起止时间。
   - 三元组清洗会过滤空实体、自环、无效类型和重复关系，非法类型回落到安全类型或通用关系。
   - LLM 提示明确禁止按相邻实体顺序连接，并优先形成符合桥梁工程语义的中心辐射结构。
   - 每条三元组保留 `document_id`、`source_text` 和 `confidence`，便于查看原文依据。

6. **知识图谱查询与可视化**
   - 支持全图、单文档图谱、节点搜索、类型过滤和 1—3 跳邻居查询。
   - 使用 ECharts 力导向图或环形布局展示节点与关系，节点大小随连接度变化。
   - 18 类节点使用稳定色板，图例同时支持中文类型和既定英文编码。
   - 节点和关系详情展示名称、类型、来源文档、原文片段、置信度等属性。
   - Neo4j 不可用时页面明确提示降级，不将 SQLite 结果伪装为 Neo4j 在线结果。

7. **桥梁档案管理**
   - 支持桥梁档案新增、查询、详情、编辑、删除、图片上传和类型统计。
   - 支持按名称、编号等条件搜索，并展示关联检测报告。
   - 删除桥梁时由数据库外键级联删除检测报告；对应业务数据不保留旧领域兼容记录。
   - 桥梁详情可触发巡检计划生成，默认结合该桥历史检测报告和适用规范。

8. **检测报告管理**
   - 支持报告新增、查询、详情、编辑、删除及多图上传和删除。
   - 支持桥梁、关键词、日期、检测类型和评定等级等筛选。
   - 提供按路线、桥梁、检测类型和等级等维度的统计数据。
   - 报告详情完整呈现检测人员、复核人员、方法、设备、指标、费用、结论和建议。

9. **模型端点管理**
   - 文本和视觉端点分别排序、启停和路由，优先级只在同一模型类型内生效。
   - 管理员通过上移/下移按钮调整顺序，后端事务化更新优先级并持久化。
   - Base URL 必须是完整 HTTP/HTTPS 地址；魔搭 API-Inference 应使用 `https://api-inference.modelscope.cn/v1`，访问令牌应填写在 API Key 字段。
   - 连接测试会区分网络连接、401 认证、404 模型/接口和 429 额度或频率限制，并保存最近错误。
   - 当主端点超时或调用失败时，LangChain 路由继续尝试下一个已启用且已配置密钥的端点。

10. **数据大屏与 SQLite 管理后台**
    - 数据大屏使用固定网格和等高图表卡片，业务数据概览、知识图谱、类型排行、趋势和任务面板底部对齐。
    - 知识图谱构建任务区域使用固定高度和内部滚动，不随任务数量改变整页布局。
    - SQLite 后台仅暴露 13 张业务表，支持表切换、关键词查询、分页、记录详情和受控删除。
    - `password_hash`、`api_key_encrypted`、短信验证码哈希等敏感字段不会通过通用管理接口展示。

11. **SQLite 业务数据模型（13 张表）**

    | 表名 | 主要用途 |
    | --- | --- |
    | `users` | 用户账号、角色、状态和头像 |
    | `folders` | 文档目录树 |
    | `documents` | 上传文档、生成型巡检计划、正文、摘要和建图状态 |
    | `chat_sessions` | 用户问答会话与标题 |
    | `chat_messages` | 问答内容、图片、检索证据、图谱快照、关键词和模型 |
    | `bridges` | 桥梁基础档案与图片 |
    | `inspection_reports` | 检测报告、多维检测信息与现场图片 |
    | `graph_triples` | 可溯源三元组及 Neo4j 降级数据 |
    | `graph_tasks` | 建图任务状态、进度和错误明细 |
    | `model_endpoints` | 文本/视觉模型端点、密钥密文和路由顺序 |
    | `system_metadata` | 初始化版本和系统元数据 |
    | `qa_feedback` | 问答评分与反馈 |
    | `sms_codes` | 验证码哈希、用途、有效期和使用状态 |

12. **主要 API 分组**

    | 前缀 | 功能 |
    | --- | --- |
    | `/api/auth/*` | 注册、密码登录、验证码发送、短信登录、短信校验、密码重置、当前用户 |
    | `/api/user/*` | 个人资料、密码和头像 |
    | `/api/admin/*` | 用户管理、统计大屏和 SQLite 白名单后台 |
    | `/api/models/*` | 模型端点 CRUD、排序和连接测试 |
    | `/api/folders/*`、`/api/documents/*` | 目录、上传解析、摘要、内容、下载和单文档建图 |
    | `/api/graph/*` | 全量/增量构建、任务、统计、三元组、搜索、邻居和可视化数据 |
    | `/api/bridges/*` | 桥梁 CRUD、图片、统计和巡检计划 |
    | `/api/inspection-reports/*` | 检测报告 CRUD、多图和统计 |
    | `/api/chat/*`、`/api/export/*` | 会话、标题、问答、流式生成、图片、反馈和 DOCX 导出 |

13. **初始化、测试与验收**
    - 后端最低支持 Python 3.11，当前项目使用 Python 3.13 独立虚拟环境验证。
    - 初始化测试数据包含 2 个用户、5 座桥梁、6 份检测报告、9 份 TXT/DOCX/PDF/XLSX 文档和 6 张桥梁现场图片。
    - 演示管理员为 `admin / 123456`，普通用户为 `inspector / 123456`；仅限本地演示，部署前必须修改。
    - 后端测试覆盖数据表与种子、四类文档解析、短信频控与真实协议、权限、头像、桥梁/报告级联、MD5 去重、模型密钥脱敏、模型排序、端点校验、标题生成、图片持久化、GraphRAG 降级、SSE 和端点故障切换。
    - 当前测试文件包含 13 个自动化用例；可运行 `./.venv/bin/python -m pytest -q backend/tests` 验证。
    - 前端可运行 `npm run build --prefix frontend` 进行 TypeScript/Vite 生产构建。
    - 视觉验收依据 `prototype` 和 `design-qa.md`，重点检查 1440、1024、768、375 像素下的布局、独立滚动、固定高度、溢出、弹窗、进度和控制台错误。

## 知识图谱节点和关系类型

为保持已有 Neo4j 数据、接口和测试的兼容性，系统保留 18 种既定英文类型编码；这些编码中的部分名称来源于早期模板，但其界面名称、抽取词典、提示词和业务语义均已映射为桥梁检测领域。前端统一展示中文名称，并按中文类型配置独立颜色。

【知识图谱节点类型（18 种）】

1. `RawMaterial`（结构材料）：混凝土、钢材、缆索、沥青混凝土等材料。
2. `CattleBreed`（桥型分类）：梁桥、拱桥、斜拉桥、悬索桥、组合梁桥等桥型。
3. `Product`（桥梁）：青江斜拉桥等具体桥梁实体，也可作为无法进一步确定类型时的安全回落类型。
4. `IntermediateProduct`（构件状态）：完好、开裂、脱空、老化、加固后状态等检测或评定状态。
5. `ProcessStep`（检测步骤）：外观检查、定期检测、无损检测、荷载试验等工作步骤。
6. `Equipment`（检测设备）：裂缝测宽仪、超声波仪、全站仪、回弹仪、传感器等设备。
7. `Location`（桥位部位）：主梁、墩台、支座区、桥面系、索塔、基础等部位。
8. `Indicator`（检测指标）：裂缝宽度、挠度、索力、碳化深度、承载能力等指标。
9. `DetectionMethod`（检测方法）：目测法、频率法、超声检测、回弹法、动静载试验等方法。
10. `PersonnelRole`（人员角色）：检测员、复核工程师、项目负责人等角色。
11. `Standard`（规范标准）：JTG、CJJ 等桥梁养护、检测与评定标准或条款。
12. `EnvironmentalCondition`（环境条件）：温度、湿度、强风、腐蚀环境、冻融环境等条件。
13. `ProductionLine`（路线路段）：国道、高速公路、城市道路及桥梁所属具体路段。
14. `Additive`（防护材料）：防水层、防腐涂层、密封胶等防护或修复材料。
15. `Microorganism`（侵蚀因素）：氯盐、冻融、碳化等影响耐久性的侵蚀因素。
16. `NutritionalComponent`（结构参数）：跨径、荷载等级、材料强度、结构尺寸等参数。
17. `PackagingMaterial`（附属设施）：支座、伸缩缝、栏杆、排水系统等附属设施。
18. `RiskEvent`（病害风险）：裂缝、锈蚀、沉降、空洞、剥落、渗水、断丝等病害或风险。

【知识图谱关系类型（20 种）】

1. `INPUT_TO`（输入到）：材料、条件或前置结果输入到检测/处治步骤。
2. `OUTPUT_FROM`（输出自）：检测结果、构件状态或结论由步骤产生。
3. `USES_EQUIPMENT`（使用检测设备）：检测步骤或方法使用某种设备。
4. `GENERATES`（生成）：检测、分析或处治过程生成结果或结论。
5. `FOLLOWS_STANDARD`（遵循规范标准）：桥梁、检测步骤、方法或病害判定遵循某规范。
6. `MONITORS_INDICATOR`（监测检测指标）：检测步骤或方法监测某项指标。
7. `CONTAINS`（包含）：桥梁或桥型包含某构件、部位或设施。
8. `LOCATED_AT`（位于）：构件、病害或设施位于某桥位部位或路段。
9. `EXECUTED_BY`（由人员执行）：检测或复核步骤由某类人员执行。
10. `USES_MATERIAL`（使用结构材料）：桥梁、桥型或构件采用某种材料。
11. `PRODUCES_INTERMEDIATE`（产生构件状态）：检测步骤产生构件状态或阶段性评定。
12. `DETECTS_BY`（通过检测方法检测）：病害或风险通过某种方法识别或复核。
13. `REQUIRES_CONDITION`（需要环境条件）：检测方法或步骤要求特定环境条件。
14. `ADDS_ADDITIVE`（添加防护材料）：桥梁或构件使用防护、修复材料。
15. `INOCULATES_MICROORGANISM`（受侵蚀因素影响）：桥梁材料或构件受到侵蚀因素作用。
16. `PROVIDES_NUTRIENT`（具有结构参数）：桥梁或桥型具有跨径、荷载等级等结构参数。
17. `USES_PACKAGING`（含附属设施）：桥梁配置支座、伸缩缝、栏杆或排水设施。
18. `MAY_CAUSE_RISK`（可能导致病害风险）：材料劣化、侵蚀因素、指标异常或既有病害可能引发风险。
19. `DOCUMENTED_IN`（记录于文档）：实体或知识关系与来源文档关联；当前构建同时在关系属性中保存来源文档 ID。
20. `RELATED_TO`（相关）：仅在缺少更精确关系且存在明确语义依据时使用的通用关系。

图谱构建必须遵守以下质量约束：关系必须有原文或直接工程语义支持；禁止自环、空实体、相邻实体机械串联和无依据的规范互联；优先使用具体关系而不是 `RELATED_TO`；所有三元组必须能够通过 `document_id` 和 `source_text` 回溯到来源资料。
